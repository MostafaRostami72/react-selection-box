import React from 'react';

const Caption = () => {
    return (
        <div className="rs-selection-caption rs-clearable-caption">
            Caption
        </div>
    );
};

export default React.memo(Caption);
